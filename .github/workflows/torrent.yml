name: Create torrent
on:
  push:
    paths:
      - ".github/workflows/torrent.yml"
      
env:
  name: "Knsn9.1080"
  comment: "Knsn91080"
  url: "https://www2186.ff-05.com/token=86-1y3IvQO1XrorTmQOKHA/1613933679/95.70.0.0/150/5/02/c20d1cff6075f35ed5cf1dd82a1ba025-1080p.mp4"
  file_name: "Knsn91080.mp4"
  piece_size: 19 # Check the README.md for recommended piece size
      
jobs:
  create:
    runs-on: ubuntu-20.04
    steps:
      - name: Install mktorrent
        run: sudo apt-get install mktorrent
        
      - name: Download file
        run: wget -nv -O "$file_name" "$url"
        
      - name: Create torrent using mktorrent
        run: mktorrent -c "$comment" -l $piece_size -o "${name}.torrent" -w "$url" "$file_name"
        
      - name: Upload torrent file
        uses: actions/upload-artifact@v2
        with:
          name: ${{ env.name }}
          path: ${{ env.name }}.torrent
